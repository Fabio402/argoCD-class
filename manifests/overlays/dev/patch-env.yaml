apiVersion: apps/v1
kind: Deployment
metadata:
  name: nest-api
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: nest-api
          env:
            - name: APP_ENV
              value: "dev"
            - name: APP_VERSION
              value: "dev"
